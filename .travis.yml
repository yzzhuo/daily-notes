language: node_js
node_js: stable

cache:
  directories:
    - node_modules
 
# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - git config user.name "yzzhuo"
  - git config user.email "yzzhuo2@gmail.com"
  - sed -i "s/gh_token/${GH_TOKEN}/g" ./_config.yml
  - hexo deploy
# E: Build LifeCycle

branches:
  only:
    - hexo
env:
 global:
   - GH_REF: github.com/yzzhuo/yzzhuo.github.io.git